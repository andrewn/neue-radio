{
  "name": "websocket",
  "version": "2.0.0",
  "description": "",
  "main": "umd.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "npm run bundle:umd",
    "bundle:umd": "rollup src/createWebsocket.js --output.format umd --output.file umd.js --output.name createWebsocket"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^4.0.0"
  },
  "devDependencies": {
    "rollup": "^0.57.1"
  }
}
